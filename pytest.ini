[pytest]
# Scope collection to tests/ and use importlib mode so a stray sibling copy of
# the lab can't trigger a "import file mismatch" on duplicate test basenames.
addopts = tests
testpaths = tests
pythonpath = .